/* use the ternary operator to log "pass" if the varaible grade
 is 60 or more, and "Failed" other wise  */

const grade = 60;

let message = grade >= 60 ? "Passed" : "Failed";
console.log(message); // Passed